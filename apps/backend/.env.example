# See root .env.example for the full list — this file exists so
# apps/backend can be run/deployed independently if the monorepo is
# ever split (Document 2, Section 10's scalability strategy).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
DATABASE_URL=
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info
